#!/usr/bin/env bash

{{- if (lookPath "pacman") }}
packages=$(pacman -Qtdq)
if [[ -n "$packages" ]]; then
    sudo pacman -Rns "$packages"
else
    echo "Nenhum pacote órfão encontrado."
fi
{{- end }}
